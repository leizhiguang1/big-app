/**
 * Fallback values for the Medical Certificate print view. The real values
 * come from `brands` (admin UI: /config/general?section=general) and the
 * issuing `outlets` row. These constants are only used when those fields
 * are blank.
 */

export const MC_DEFAULT_LOGO_PATH = "/mc-logo.svg";
export const MC_FOOTER_NOTE = "*Not valid for absence from court case";
